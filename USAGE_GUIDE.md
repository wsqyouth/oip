# OIP 项目使用指南

## 🚀 日常开发流程

### 场景 1: 开发新功能

```bash
# 1. 进入项目目录
cd /Users/cooperswang/Documents/wsqyouth/oip/backend

# 2. 创建功能分支（可选）
git checkout -b feature/story-001

# 3. 开发代码...

# 4. 代码完成后，自动检查会触发（如果配置了 Claude Code hooks）
# 或手动运行检查：
bash ../.claude/hooks/post-tool-use.sh

# 5. 确认无误后，一键提交
bash ../.claude/commands/commit-push.sh "feat: 实现基础设施搭建"
```

### 场景 2: 修复 Bug

```bash
cd /Users/cooperswang/Documents/wsqyouth/oip/backend

# 修复代码...

# 提交
bash ../.claude/commands/commit-push.sh "fix: 修复 Redis 连接泄漏问题"
```

---

## 📖 智能提交命令使用指南

### 可用命令

#### 1. commit.sh - 增强版智能提交（推荐）

支持 rebase、amend 等高级操作的智能提交命令。

**基本用法**:
```bash
bash ./.claude/commands/commit.sh "commit message" [选项]
```

**可用选项**:
- `--rebase [branch]`: 在提交前 rebase 到指定分支（默认: main）
- `--amend`: 修改上次提交
- `--no-verify`: 跳过代码检查（不推荐）
- `--push`: 提交后自动推送（跳过确认）

**使用示例**:
```bash
# 基础提交
bash ./.claude/commands/commit.sh "feat: 实现订单创建API"

# Rebase 到 main 后提交
bash ./.claude/commands/commit.sh "feat: 实现订单创建API" --rebase

# Rebase 到指定分支后提交
bash ./.claude/commands/commit.sh "feat: 实现订单创建API" --rebase develop

# 修改上次提交
bash ./.claude/commands/commit.sh "fix: 修复订单API" --amend

# 提交后自动推送
bash ./.claude/commands/commit.sh "feat: 新功能" --push

# Rebase 并自动推送
bash ./.claude/commands/commit.sh "feat: 完整流程" --rebase --push
```

**执行流程**:
```
$ bash ./.claude/commands/commit.sh "feat: 实现订单创建API" --rebase

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  智能提交流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 执行代码检查...
  → 格式化代码... ✅
  → 静态检查... ✅ (或提示问题，询问是否继续)
  → 运行单元测试... ✅ (或提示失败，询问是否继续)
  → 整理依赖... ✅
✅ 代码检查通过

🔄 执行 Rebase 操作...
  → 当前分支: feature/story-001
  → 目标分支: main
  → 拉取最新 main...
  → Rebase 到 main...
✅ Rebase 成功

📊 Git 状态:
M  backend/PRD.md
M  backend/.claude.md
A  backend/stories/story-001.md

📝 提交代码...
确认提交以上文件？(y/n) y ← 需要你确认

✅ 代码已提交: feat: 实现订单创建API

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 下一步操作：
   推送到远程: git push
   强制推送: git push --force-with-lease (推荐)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

是否现在推送到远程？(y/n) y ← 需要你确认

⚠️  检测到 rebase，使用 --force-with-lease 推送
确认强制推送？(y/n) y ← 需要你再次确认

✅ 代码已推送到远程

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ✨ 提交流程完成！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### 2. commit-push.sh - 简化版快捷提交

基础的提交推送命令，适合简单场景。

**基本用法**:
```bash
bash ./.claude/commands/commit-push.sh "feat: 实现订单创建API"
```

### 交互式确认点

1. **静态检查失败时**：
   ```
   ⚠️  静态检查发现问题
   是否继续提交？(y/n)
   ```

2. **测试失败时**：
   ```
   ⚠️  测试失败
   是否继续提交？(y/n)
   ```

3. **提交前确认**：
   ```
   📊 Git 状态:
   M  file1.go
   M  file2.md
   
   确认提交以上文件？(y/n)
   ```

---

## 🎯 实际使用示例

### 示例 1: 完成 Story-001

```bash
# 1. 进入后端目录
cd /Users/cooperswang/Documents/wsqyouth/oip/backend

# 2. 确认当前工作
cat stories/story-001-基础设施搭建.md

# 3. 完成开发后提交
bash ../.claude/commands/commit-push.sh "feat: 完成基础设施搭建

- 添加 docker-compose.yml
- 创建数据库初始化脚本
- 添加连接测试脚本

Story-001"
```

### 示例 2: 只检查不提交

```bash
# 仅运行代码检查
bash ../.claude/hooks/post-tool-use.sh
```

---

## 🔧 故障排查

### 问题 1: 提示 "go.mod file not found"

**原因**: 不在正确的目录

**解决**: 
```bash
cd /Users/cooperswang/Documents/wsqyouth/oip/backend
```

### 问题 2: Git push 失败

**原因**: 未配置远程仓库

**解决**:
```bash
# 查看远程仓库
git remote -v

# 添加远程仓库
git remote add origin <your-repo-url>
```

### 问题 3: 测试失败但想强制提交

**操作**: 当脚本询问时，输入 `y` 确认继续

---

## 💡 最佳实践

### ✅ 推荐做法

1. **每次开发前**：
   - 阅读 `backend/.claude.md` 理解上下文
   - 查看对应的 Story 文件

2. **开发过程中**：
   - 小步提交，频繁提交
   - 提交信息清晰描述改动

3. **提交前**：
   - 运行 `post-tool-use.sh` 确保代码格式正确
   - 确保测试通过

### ❌ 避免做法

1. **不要**：跳过代码检查直接 `git push`
2. **不要**：提交未格式化的代码
3. **不要**：提交未测试的代码
4. **不要**：使用无意义的提交信息（如 "update", "fix bug"）

---

## 📚 相关文档

- [项目总览](README.md)
- [后端 PRD](backend/PRD.md)
- [后端开发指南](backend/.claude.md)
- [架构知识](/.claude/knowledge/architecture.md)
- [代码规范](/.claude/knowledge/coding-standards.md)
- [常见错误](/.claude/knowledge/common-mistakes.md)
